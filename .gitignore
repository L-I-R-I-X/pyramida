# Examples 1:
# Project with only source/config files - no artifacts present
# Expected output: (empty - output nothing)

# Example 2:
# Project with artifacts like pycache, dist, .env.local, .vscode
# Expected output includes compiled files, dependencies, editor configs, etc.